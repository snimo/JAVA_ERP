package com.srn.erp.cip.bm;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.util.Date;

public class Logger {
	
	private static Logger logger;
	
	private Logger () {
	}
	
	public synchronized static Logger getLogger(){
		if(logger == null){
			logger = new Logger();
		}
		return logger;
	}
	
	public synchronized void grabarMensaje(String mensaje, String path){
		if(true)
			return;
//		if(path == null || path.isEmpty())
//			return;
//		
//		FileWriter o = null;
//		String separator =  System.getProperty("line.separator");
//		Date date = new Date();
//				
//		try {
//			o = new FileWriter(new File(path), true);
//			o.write(mensaje +" " + date.toString() + separator);
//		} catch (IOException e) {
//			System.out.println("Error: " + e.getMessage());
//		} finally {
//			if (o != null) {
//				try {
//					o.close();
//				} catch (IOException e) {
//					System.out.println("Error al cerrar: " + e.getMessage());
//				}
//			}
//
//		}
	}
}
